[package]
name = "enum-variant-kind-derive"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "1.0", features = [
	"derive",
	"parsing",
	"full",
	"extra-traits",
] }
quote = "1.0"
proc-macro2 = "1.0"
